/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import FormButton from './FormButton';

storiesOf('FormButton', module).add('default', () => <FormButton />);
